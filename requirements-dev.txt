# =============================================================================
# requirements-dev.txt — deps PESADAS de pipeline local (não vão pra prod).
# Use apenas pra desenvolvimento ou benchmark Tier B/C com YOLO/Qwen/Whisper.
#
# Instalar:
#   pip install -r requirements.txt -r requirements-dev.txt
# =============================================================================

# OpenCV — processamento local de frames (src/detectors/*, src/ingestion/*)
opencv-python==4.10.0.84

# Whisper local (transcrição de áudio sem chamar Vertex).
# Puxa transitivamente ctranslate2 + av + onnxruntime — ~294 MB.
faster-whisper==1.0.3

# Trilha A: Mac (M1/M2/M3) com MLX
# mlx==0.18.0
# mlx-vlm==0.1.10

# Trilha B: Linux + CUDA (Runpod, servidores)
# torch==2.4.0
# transformers==4.45.2
# accelerate==0.34.2
# qwen-vl-utils==0.0.8

# Tooling CVAT (opcional)
# ultralytics==8.3.0   # YOLO-NAS pre-anotações
